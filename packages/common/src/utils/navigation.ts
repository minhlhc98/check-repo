// import { ISidebarItem } from "../constants";

// export const getBreadcrumbTitle = (
//   url: string,
//   urlArray: Array<ISidebarItem>,
//   title: string
// ): string | null => {
//   for (const item of urlArray) {
//     if (item.url && item.url === url) return item.title;
//     if (Array.isArray(item.items) && item.items.length > 0) {
//       return getBreadcrumbTitle(url, item.items, title);
//     }
//   }
//   return null;
// };
